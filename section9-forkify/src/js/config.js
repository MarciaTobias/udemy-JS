// that one is better than fetch, return automaticaly json
// In that case is not a big deal, but never store any real sensitive data like this(key),
// on the JavaScript cliente sidee
export const key = 'fc9c16f3c0fdccce9fd63e9003aeb034';